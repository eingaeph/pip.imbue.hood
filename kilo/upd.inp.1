0000 KILO #! /usr/bin/tcc -run 
$I /* Kilo -- A very simple editor in less than 1-kilo lines of code (as counted
0040 KILO #include <time.h>
0066 KILO
$I FILE *fStream;
$I int ignore;
$I
$I void init(void)
$I {
$I     fStream = fopen("/dev/pts/18", "w+");
$I     int ignore = 0;
$I }
$I
$I int nucat( const char infil[] )
$I    {
$I     FILE *fPtr;
$I      fPtr=fopen(infil, "r");
$I      char ClearScreen[] = "\x1b[2J";
$I        fwrite(&ClearScreen,1,5,fStream);
$I
$I      int ch;
$I while
$I      ((ch = fgetc(fPtr)) != EOF)
$I         fwrite(&ch,1,1,fStream);   
$I 
$I      fclose(fPtr);
$I      return 0;
$I }
1184 KILO         exit(0);  /* The sole no-error return from kilo */
1260 KILO    init();   
$I      initEditor(); $$

