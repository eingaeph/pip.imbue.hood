#include "../libk.h"

#define extern

int main(void)
{ 

//  enter / leave raw mode to avoid echoing to the screen, 
//  when the terminal sends information about its size

    enableRawMode(); uvset(); disableRawMode(); return 0;
    screenTest();

    return 0;
 }
